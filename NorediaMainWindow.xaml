<Window x:Class="Noredia.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Noredia"
        Height="650" Width="430"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- ===== RENK PALETİ ===== -->
        <SolidColorBrush x:Key="BgDark"     Color="#0C0C11"/>
        <SolidColorBrush x:Key="BgCard"     Color="#15151E"/>
        <SolidColorBrush x:Key="BgInput"    Color="#1B1B26"/>
        <SolidColorBrush x:Key="BgHover"    Color="#22222F"/>
        <SolidColorBrush x:Key="BorderCol"  Color="#282838"/>
        <SolidColorBrush x:Key="Accent"     Color="#F2813F"/>
        <SolidColorBrush x:Key="AccentDim"  Color="#3D1A12"/>
        <SolidColorBrush x:Key="TextMain"   Color="#E8E8F0"/>
        <SolidColorBrush x:Key="TextDim"    Color="#62627A"/>
        <SolidColorBrush x:Key="TextMuted"  Color="#3E3E54"/>

        <!-- Ateş Gradient — Normal -->
        <LinearGradientBrush x:Key="FireBrush" StartPoint="0,0.5" EndPoint="1,0.5">
            <GradientStop Color="#D4421A" Offset="0"/>
            <GradientStop Color="#E8773A" Offset="0.5"/>
            <GradientStop Color="#F2A93B" Offset="1"/>
        </LinearGradientBrush>

        <!-- Ateş Gradient — Hover -->
        <LinearGradientBrush x:Key="FireBrushHover" StartPoint="0,0.5" EndPoint="1,0.5">
            <GradientStop Color="#F2A93B" Offset="0"/>
            <GradientStop Color="#FFD966" Offset="0.45"/>
            <GradientStop Color="#FFB347" Offset="1"/>
        </LinearGradientBrush>

        <!-- Ateş Gradient — Format Seçili -->
        <LinearGradientBrush x:Key="FireBrushSmall" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#D44A1A" Offset="0"/>
            <GradientStop Color="#F2A93B" Offset="1"/>
        </LinearGradientBrush>

        <!-- ===== COMPACT TEXTBOX ===== -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background"      Value="{StaticResource BgInput}"/>
            <Setter Property="Foreground"      Value="{StaticResource TextMain}"/>
            <Setter Property="CaretBrush"      Value="{StaticResource Accent}"/>
            <Setter Property="FontSize"        Value="12.5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="11,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                BorderBrush="{StaticResource BorderCol}"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center"
                                          SnapsToDevicePixels="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== ATEŞ ACCENT BUTON (ANİMASYONLU) ===== -->
        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="Foreground"      Value="#0C0C11"/>
            <Setter Property="FontWeight"      Value="Bold"/>
            <Setter Property="FontSize"        Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <!-- Glow katmanı -->
                            <Border x:Name="GlowBorder"
                                    CornerRadius="14"
                                    Background="Transparent"
                                    Margin="-4"
                                    Opacity="0.3">
                                <Border.Effect>
                                    <DropShadowEffect Color="#E84A1C" BlurRadius="25" ShadowDepth="0" Opacity="0.6"/>
                                </Border.Effect>
                            </Border>
                            <!-- Ana buton -->
                            <Border x:Name="Bd"
                                    Background="{StaticResource FireBrush}"
                                    CornerRadius="12"
                                    Padding="0,11"
                                    SnapsToDevicePixels="True">
                                <Border.Effect>
                                    <DropShadowEffect Color="#E84A1C" BlurRadius="14" ShadowDepth="0" Opacity="0.35"/>
                                </Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource FireBrushHover}"/>
                                <Setter TargetName="Bd" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FFB347" BlurRadius="35" ShadowDepth="0" Opacity="0.8"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="GlowBorder" Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Bd" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#D4421A" BlurRadius="8" ShadowDepth="0" Opacity="0.2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentDim}"/>
                                <Setter TargetName="Bd" Property="Effect" Value="{x:Null}"/>
                                <Setter TargetName="GlowBorder" Property="Visibility" Value="Collapsed"/>
                                <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Üzerine gelince büyüme animasyonu -->
                <EventTrigger RoutedEvent="Mouse.MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.03" Duration="0:0:0.15">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.03" Duration="0:0:0.15">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <!-- Ayrılınca eski boyut -->
                <EventTrigger RoutedEvent="Mouse.MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.15"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.15"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <!-- ===== GHOST BUTON (ANİMASYONLU) ===== -->
        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"      Value="{StaticResource TextDim}"/>
            <Setter Property="FontSize"        Value="11"/>
            <Setter Property="FontWeight"      Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="12,7"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Mouse.MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="Mouse.MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <!-- ===== BAŞLIK BUTONU ===== -->
        <Style x:Key="TitleBtn" TargetType="Button">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"      Value="{StaticResource TextDim}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Width="30" Height="30" CornerRadius="7" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== FORMAT TOGGLE ===== -->
        <Style x:Key="FmtBtn" TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource BgCard}"/>
            <Setter Property="Foreground"      Value="{StaticResource TextDim}"/>
            <Setter Property="FontSize"        Value="11.5"/>
            <Setter Property="FontWeight"      Value="Normal"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="9"
                                BorderBrush="{StaticResource BorderCol}"
                                BorderThickness="1.2"
                                Padding="0,9"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Mouse.MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.08" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="Mouse.MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <!-- ===== PROGRESS BAR ===== -->
        <Style x:Key="ModernProgress" TargetType="ProgressBar">
            <Setter Property="Height"     Value="6"/>
            <Setter Property="Background" Value="{StaticResource BgInput}"/>
            <Setter Property="Foreground" Value="{StaticResource Accent}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border x:Name="PART_Track" Background="{TemplateBinding Background}" CornerRadius="3" Height="{TemplateBinding Height}"/>
                            <Border x:Name="PART_Indicator" HorizontalAlignment="Left" Background="{TemplateBinding Foreground}" CornerRadius="3" Height="{TemplateBinding Height}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== COMPACT COMBO ===== -->
        <Style x:Key="ModernCombo" TargetType="ComboBox">
            <Setter Property="Background"      Value="{StaticResource BgInput}"/>
            <Setter Property="Foreground"      Value="{StaticResource TextMain}"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="Padding"         Value="12,8"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BorderCol}"/>
            <Setter Property="BorderThickness" Value="1.2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="10" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                <Grid>
                                    <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="2,0"/>
                                    <Path Data="M0,0 L4,4 L8,0" Stroke="{StaticResource TextDim}" StrokeThickness="1.5" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                </Grid>
                            </Border>
                            <ToggleButton x:Name="Toggle" Focusable="False" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" PopupAnimation="Slide">
                                <Border Background="{StaticResource BgCard}" CornerRadius="10" BorderBrush="{StaticResource BorderCol}" BorderThickness="1.2" Margin="0,3,0,0" MaxHeight="180">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"      Value="{StaticResource TextMain}"/>
            <Setter Property="Padding"         Value="12,7"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="5" Margin="3,1">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentDim}"/>
                                <Setter Property="Foreground" Value="{StaticResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- ===== ANA GÖVDE ===== -->
    <Border CornerRadius="16"
            Background="{StaticResource BgDark}"
            BorderBrush="{StaticResource BorderCol}"
            BorderThickness="1"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" ShadowDepth="2" Opacity="0.5" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ═══ BAŞLIK ÇUBUĞU ═══ -->
            <Grid Grid.Row="0" MouseLeftButtonDown="TitleBar_Drag" Background="Transparent">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="18,0">
                    <Ellipse Width="7" Height="7" Fill="{StaticResource Accent}" Margin="0,0,8,0"/>
                    <TextBlock Text="Noredia" Foreground="{StaticResource TextMain}" FontSize="11" FontWeight="Black" FontFamily="Segoe UI"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,6,0">
                    <Button x:Name="BtnMin" Click="BtnMin_Click" Style="{StaticResource TitleBtn}" Content="─" FontSize="9" FontWeight="Bold"/>
                    <Button x:Name="BtnClose" Click="BtnClose_Click" Style="{StaticResource TitleBtn}" Content="✕" FontSize="11" Margin="1,0,0,0"/>
                </StackPanel>
            </Grid>

            <!-- ═══ İÇERİK (SCROLLVIEWER YOK - SABİT LAYOUT) ═══ -->
            <Grid Grid.Row="1" Margin="22,0,22,22">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Başlık -->
                <TextBlock Grid.Row="0" Text="Medya İndirici" Foreground="{StaticResource TextMain}" FontSize="22" FontWeight="Black" HorizontalAlignment="Center" Margin="0,0,0,3"/>
                <TextBlock Grid.Row="1" Text="YouTube · Instagram · TikTok · Twitter · ve 1800+ site" Foreground="{StaticResource TextMuted}" FontSize="10" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- URL -->
                <TextBlock Grid.Row="2" Text="LİNK" Foreground="{StaticResource TextDim}" FontSize="9" FontWeight="SemiBold" Margin="0,0,0,5"  /> 
                <Grid Grid.Row="3" Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="310"/> 
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TxtUrl" Style="{StaticResource ModernTextBox}" TextChanged="TxtUrl_TextChanged"/>
                    <Button Grid.Column="1" x:Name="BtnPaste" Click="BtnPaste_Click" Style="{StaticResource GhostButton}" Content="Yapıştır" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </Grid>

                <Border x:Name="PlatformBadge" Grid.Row="4" CornerRadius="6" Background="{StaticResource BgCard}" BorderBrush="{StaticResource BorderCol}" BorderThickness="1" Padding="10,4" HorizontalAlignment="Left" Margin="0,2,0,16" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="PlatformIcon" FontSize="10" Margin="0,0,5,0" Foreground="{StaticResource Accent}" VerticalAlignment="Center" FontWeight="Bold"/>
                        <TextBlock x:Name="PlatformName" FontSize="10.5" Foreground="{StaticResource TextMain}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <!-- FORMAT -->
                <TextBlock Grid.Row="5" Text="FORMAT" Foreground="{StaticResource TextDim}" FontSize="9" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <UniformGrid Grid.Row="6" Columns="3" Margin="0,0,0,14">
                    <Button x:Name="FmtVideo" Click="Fmt_Click" Tag="Video" Content="Video" Style="{StaticResource FmtBtn}" Margin="0,0,4,0" Background="#FF15151E"/>
                    <Button x:Name="FmtAudio" Click="Fmt_Click" Tag="Audio" Content="Ses / MP3" Style="{StaticResource FmtBtn}" Margin="2,0" Background="#FF15151E" BorderBrush="#FF707070" Foreground="#FF62627A"/>
                    <Button x:Name="FmtImage" Click="Fmt_Click" Tag="Image" Content="Resim" Style="{StaticResource FmtBtn}" Margin="4,0,0,0"/>
                </UniformGrid>

                <!-- KALİTE -->
                <TextBlock Grid.Row="7" Text="KALİTE" Foreground="{StaticResource TextDim}" FontSize="9" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox x:Name="CmbQuality" Grid.Row="8" Style="{StaticResource ModernCombo}" Margin="0,0,0,16">
                    <ComboBoxItem Content="En İyi (Önerilen)" IsSelected="True"/>
                    <ComboBoxItem Content="2160p (4K)"/>
                    <ComboBoxItem Content="1080p"/>
                    <ComboBoxItem Content="720p"/>
                    <ComboBoxItem Content="480p"/>
                    <ComboBoxItem Content="360p"/>
                </ComboBox>

                <!-- İNDİR BUTONU -->
                <Button x:Name="BtnDownload" Grid.Row="9" Click="BtnDownload_Click" Style="{StaticResource AccentButton}" Content="⬇  İNDİR" VerticalAlignment="Top" Margin="0,0,0,10"/>

                <!-- İLERLEME VE GEÇMİŞ ALANI -->
                <Grid Grid.Row="10" VerticalAlignment="Bottom">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid x:Name="ProgressArea" Visibility="Collapsed" Margin="0,0,0,10">
                        <ProgressBar x:Name="Progress" Style="{StaticResource ModernProgress}" Margin="0,0,0,5"/>
                        <TextBlock x:Name="StatusText" Foreground="{StaticResource TextDim}" FontSize="11" HorizontalAlignment="Center" Margin="0,12,0,0"/>
                    </Grid>

                    <Border x:Name="HistoryArea" Grid.Row="1" Height="1" Background="{StaticResource BorderCol}" Margin="0,4,0,12"/>

                    <Grid Grid.Row="2" Margin="0,0,0,8">
                        <TextBlock Text="Son İndirme" Foreground="{StaticResource TextMain}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </Grid>

                    <Border x:Name="LastDownloadCard" Grid.Row="3" Background="{StaticResource BgCard}" CornerRadius="10" Padding="12,10" BorderBrush="{StaticResource BorderCol}" BorderThickness="1" Visibility="Collapsed">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock x:Name="LastUrl" Foreground="{StaticResource TextMain}" FontSize="11" TextTrimming="CharacterEllipsis" MaxWidth="280"/>
                                <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                    <Border Background="#1E1E2E" CornerRadius="4" Padding="6,2" Margin="0,0,6,0">
                                        <TextBlock x:Name="LastFormat" FontSize="9" Foreground="{StaticResource Accent}" FontWeight="SemiBold"/>
                                    </Border>
                                    <TextBlock x:Name="LastStatus" FontSize="10" Foreground="{StaticResource TextDim}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                            <TextBlock x:Name="LastProgressTxt" Grid.Column="1" VerticalAlignment="Center" Foreground="{StaticResource Accent}" FontWeight="Bold" FontSize="12.5"/>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="4" Margin="0,18,0,0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <Button x:Name="BtnSettings" Click="BtnSettings_Click" Style="{StaticResource GhostButton}" Content="Ayarlar"/>
                        </StackPanel>
                        <Button x:Name="BtnOpenFolder" Click="BtnOpenFolder_Click" Style="{StaticResource GhostButton}" Content="Klasörü Aç" HorizontalAlignment="Right"/>
                    </Grid>
                </Grid>
            </Grid>

            <!-- ═══ AYARLAR POPUP (Kaplama) ═══ -->
            <Grid x:Name="SettingsOverlay" Grid.Row="0" Grid.RowSpan="2" Background="#B0000000" Visibility="Collapsed" Margin="8">
                <Border Background="{StaticResource BgCard}" CornerRadius="16" Padding="25" Width="340" VerticalAlignment="Center" HorizontalAlignment="Center" BorderBrush="{StaticResource BorderCol}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Noredia Ayarları" Foreground="{StaticResource TextMain}" FontSize="18" FontWeight="Black" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                        <Border Height="1" Background="{StaticResource BorderCol}" Margin="0,0,0,15"/>

                        <TextBlock Text="Durum" Foreground="{StaticResource Accent}" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock x:Name="SettingsCookieStatus" Text="Cookie durumu yükleniyor..." Foreground="{StaticResource TextDim}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,12"/>

                        <TextBlock Text="İndirme Klasörü" Foreground="{StaticResource Accent}" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock x:Name="SettingsFolderPath" Text="Yükleniyor..." Foreground="{StaticResource TextDim}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,12"/>

                        <TextBlock Text="Instagram Kurulumu" Foreground="{StaticResource Accent}" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock Foreground="{StaticResource TextDim}" FontSize="10.5" TextWrapping="Wrap" LineHeight="18">
                            1. Chrome'da Instagram'a giriş yapın<LineBreak/>
                            2. "Get cookies.txt LOCALLY" eklentisini kurun<LineBreak/>
                            3. Instagram sayfasındayken cookies dışa aktarın<LineBreak/>
                            4. cookies.txt dosyasını uygulamanın yanına kopyalayın
                        </TextBlock>

                        <Button Click="CloseSettings_Click" Style="{StaticResource AccentButton}" Content="KAPAT" Margin="0,20,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>